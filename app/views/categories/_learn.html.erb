
<%= form_for([category, @lesson]) do |f| %>
    <%= hidden_field_tag :current_user, current_user.id %>
    <%# link_to "LEARN", new_lesson_word_path(@lesson), class: "btn btn-primary"%>
    <%= f.submit "LEARN", class: "btn btn-primary" %>
<% end %>